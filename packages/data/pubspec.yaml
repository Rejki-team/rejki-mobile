name: data
description: Data layer - Models, data sources, and repository implementations
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.4
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  equatable: ^2.0.7
  fpdart: ^1.1.0
  dio: ^5.4.0
  injectable: ^2.5.0
  # Internal packages
  domain:
    path: ../domain
  network:
    path: ../network
  local:
    path: ../local
  core:
    path: ../core

dev_dependencies:
  build_runner: ^2.4.7
  freezed: ^3.2.3
  json_serializable: ^6.8.0
  lints: ^6.0.0
  test: ^1.25.6
