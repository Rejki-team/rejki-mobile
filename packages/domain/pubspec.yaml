name: domain
description: Domain layer - Business logic, entities, and repository interfaces
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.4
resolution: workspace

dependencies:
  freezed_annotation: ^3.1.0
  fpdart: ^1.1.0
  equatable: ^2.0.7
  injectable: ^2.5.0
  # Internal packages
  core:
    path: ../core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  build_runner: ^2.10.4
  freezed: ^3.2.3
