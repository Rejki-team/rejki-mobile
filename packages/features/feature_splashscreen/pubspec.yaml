name: feature_splashscreen
description: "Splash screen feature module"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_native_splash: ^2.4.7
  go_router: ^17.0.1
  freezed_annotation: ^3.1.0
  get_it: ^8.0.3
  # Internal packages
  designsystems:
    path: ../../designsystems
  network:
    path: ../../network
  local:
    path: ../../local

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.10.4
  freezed: ^3.2.3
