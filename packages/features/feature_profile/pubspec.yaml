name: feature_profile
description: "Profile feature module"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  go_router: ^17.0.1
  get_it: ^8.0.3
  camera: ^0.11.0+2
  # Internal packages
  designsystems:
    path: ../../designsystems
  components:
    path: ../../components
  local:
    path: ../../local
  flutter_svg: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
